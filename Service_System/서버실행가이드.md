# Django 서버 실행 가이드

## 서버 실행 방법

```bash
cd "/Users/choikjun/5-2/모웹프/학기말 프로젝트/Service_System"
source venv/bin/activate
python manage.py runserver
```

서버가 실행되면:
- **서버 주소**: http://127.0.0.1:8000
- **관리자 페이지**: http://127.0.0.1:8000/admin/
- **API 엔드포인트**: http://127.0.0.1:8000/api/logs/

## 서버 중지

터미널에서 `Ctrl + C`를 누르면 서버가 종료됩니다.

## 관리자 계정 설정

서버 실행 전에 관리자 계정을 설정하세요:

### 방법 1: createsuperuser (권장)
```bash
python manage.py createsuperuser
```

### 방법 2: 기존 사용자 비밀번호 재설정
```bash
python manage.py shell
```

Python shell에서:
```python
from django.contrib.auth.models import User
user = User.objects.get(username='admin')
user.set_password('admin123')
user.is_superuser = True
user.is_staff = True
user.save()
exit()
```

## 현재 Token 정보

- **Token**: `3aaa6f4666681d72f9aeb065a6074b9c3c1613e1`
- **Username**: `admin`
- Edge System의 `detect.py`에 이미 설정되어 있습니다.

## 유용한 명령어

### 마이그레이션 확인
```bash
python manage.py showmigrations
```

### 데이터베이스 초기화 (주의: 모든 데이터 삭제)
```bash
rm db.sqlite3
python manage.py migrate
```

### 서버 상태 확인
브라우저에서 http://127.0.0.1:8000 접속하여 확인

